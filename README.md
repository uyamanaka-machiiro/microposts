# microposts

開発オンボーディングで作った MicropostApp

## commands

### setup

```sh
# init api
$ cd ./api
$ bundle install

# init npm
$ cd ../web
$ yarn install

# run redis server
$ docker-compose up -d
```

### start

```sh
# start backend
$ rails s

# start frontend
$ yarn dev
```
